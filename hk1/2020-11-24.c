#include <stdio.h>

int main() {
	command(detail) {
		...
	}

	int x=0;

	if(x<3) {
		printf('a');
	} if(x>6) {
		printf('b');
	} else {
		printf('deo phai a');
	}

	while(x<10) {
		printf('dcmm');
		x++
		x+=1;
		x=x+1;
	}
}
